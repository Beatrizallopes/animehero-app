/** @type {import('next').NextConfig} */
// const nextConfig = {};
const nextConfig = {
    images: {
      domains: ['media.kitsu.io'], // Adicione o domínio da sua imagem aqui
    },
  };

export default nextConfig;
